simpleArgs: &simple-args
  numberOfAccounts: &number-of-accounts 50
  tps: &tps 50
  workers: &workers 1

test:
  name: CBSDRegistry-benchmark
  description: Benchmark for CBSDRegistry smart contract
  workers:
    type: local
    number: *workers
  rounds:
    - label: registerCBSD
      txNumber: *number-of-accounts
      rateControl:
        type: fixed-rate
        opts:
          tps: *tps
      workload:
        module: benchmarks/scenario/CBSDRegistry/register.js
    - label: updateGrantAmount
      txNumber: *number-of-accounts
      rateControl:
        type: fixed-rate
        opts:
          tps: *tps
      workload:
        module: benchmarks/scenario/CBSDRegistry/updateGrant.js
    - label: updateStatus
      txNumber: *number-of-accounts
      rateControl:
        type: fixed-rate
        opts:
          tps: *tps
      workload:
        module: benchmarks/scenario/CBSDRegistry/updateStatus.js
    - label: getCBSDInfo
      txNumber: *number-of-accounts
      rateControl:
        type: fixed-rate
        opts:
          tps: *tps
      workload:
        module: benchmarks/scenario/CBSDRegistry/query.js

monitors:
  resource:
    - module: prometheus
      options:
        url: "http://localhost:9090"
        metrics:
          include: ["4node-qbft-validator1-1", "4node-qbft-validator2-1", "4node-qbft-validator3-1", "4node-qbft-validator4-1"]
          queries:
            - name: Besu CPU Usage (cores)
              query: sum(rate(container_cpu_usage_seconds_total{name=~"4node-qbft-validator[1-4]-1"}[30s])) by (name)
              step: 10
              label: name
              statistic: avg
            - name: Besu Memory Usage (MB)
              query: sum(container_memory_usage_bytes{name=~"4node-qbft-validator[1-4]-1"}) by (name)
              step: 10
              label: name
              statistic: max
              multiplier: 0.000001
            - name: Besu Disk Usage (MB)
              query: sum(container_fs_usage_bytes{name=~"4node-qbft-validator[1-4]-1"}) by (name)
              step: 10
              label: name
              statistic: max
              multiplier: 0.000001
            # - name: cAdvisor CPU Usage (cores)
            #   query: sum(rate(container_cpu_usage_seconds_total{name="cadvisor"}[30s])) by (name)
            #   step: 10
            #   label: name
            #   statistic: avg
            # - name: cAdvisor Memory Usage (MB)
            #   query: sum(container_memory_usage_bytes{name="cadvisor"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
            # - name: cAdvisor Disk Usage (MB)
            #   query: sum(container_fs_usage_bytes{name="cadvisor"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
            # - name: Prometheus CPU Usage (cores)
            #   query: sum(rate(container_cpu_usage_seconds_total{name="prometheus"}[30s])) by (name)
            #   step: 10
            #   label: name
            #   statistic: avg
            # - name: Prometheus Memory Usage (MB)
            #   query: sum(container_memory_usage_bytes{name="prometheus"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
            # - name: Prometheus Disk Usage (MB)
            #   query: sum(container_fs_usage_bytes{name="prometheus"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
            # - name: Grafana CPU Usage (cores)
            #   query: sum(rate(container_cpu_usage_seconds_total{name="grafana"}[30s])) by (name)
            #   step: 10
            #   label: name
            #   statistic: avg
            # - name: Grafana Memory Usage (MB)
            #   query: sum(container_memory_usage_bytes{name="grafana"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
            # - name: Grafana Disk Usage (MB)
            #   query: sum(container_fs_usage_bytes{name="grafana"}) by (name)
            #   step: 10
            #   label: name
            #   statistic: max
            #   multiplier: 0.000001
        charting:
          bar:
            metrics: [all] 